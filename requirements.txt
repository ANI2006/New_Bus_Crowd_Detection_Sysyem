# Web framework
flask>=3.0.0
flask-socketio>=5.3.6
python-socketio>=5.10.0
eventlet>=0.35.2

# Computer vision
opencv-python>=4.9.0

# YOLOv8 (also installs torch, torchvision, numpy, pillow, pyyaml, tqdm, scipy)
ultralytics>=8.2.0
